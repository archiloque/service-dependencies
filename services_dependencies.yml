# Database settings.
database:
  driverClass: org.postgresql.Driver
  user: services-dependencies
  password: services-dependencies
  url: jdbc:postgresql://localhost:5432/services-dependencies
  properties:
    charSet: UTF-8
    hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
